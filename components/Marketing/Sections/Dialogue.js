import React from 'react'
import { css } from 'glamor'
import { Container } from '@project-r/styleguide'

export default function DialogSection({ t }) {
  return <Container {...styles.container}></Container>
}

const styles = {
  container: css({})
}
